# Система для проверки OpenGraph разметки

Система предназначена для проверки валидности OpenGraph разметки в первую очередь в тестовом/локальном окружении.

## Возможности системы

Система должна иметь возможность работать в двух режимах

1) Режим локальной проверки
2) Режим фактической серверной проверки

### Режим локальной проверки

В режиме локальной проверки происходит проверка сайта по заданному заранее набору параметров. Пользователь должен иметь возможность настраивать социальные сети, для которых он хочет проверить разметку — набор проверяемых тегов и правил для каждой соцсети задаётся через конфиг (см. раздел «Конфигурация проверки»).

Расширение можно глобально включать/выключать переключателем (ползунком) — в popup и/или на странице настроек. В выключенном состоянии проверки не выполняются, badge серый. Состояние сохраняется в `storage`.

Область проверки настраивается через один из двух взаимоисключающих режимов (белый и чёрный списки не используются одновременно):

1) **Все, кроме списка (чёрный список)** — режим по умолчанию; проверка на всех сайтах, кроме перечисленных; список по умолчанию пуст (= проверять везде). В popup на текущем сайте доступна кнопка **«Отключить для этого сайта»** — добавляет домен активной вкладки в чёрный список.
2) **Только из списка (белый список)** — проверка только на перечисленных доменах. В popup доступна кнопка **«Включить на этом сайте»** — добавляет домен активной вкладки в белый список. (Режим «только текущий домен» отдельно не нужен — это частный случай белого списка с одним доменом.)

Оба списка доменов хранятся отдельно и **не сбрасываются при переключении режима**: в настройках всегда виден полный список для каждого режима, переключение лишь меняет, какой из них активен. Списки поддерживают wildcard/поддомены/порты (важно для `localhost`). Настройки хранятся в `storage`.

Результат проверки имеет три уровня: `ok`, `warning`, `error`. Индикация состоянием badge на иконке расширения:

- **серый** — проверка выключена / не выполнялась;
- **синий** — проверка в процессе;
- **зелёный** — всё ок (`ok`);
- **жёлтый** — есть замечания (`warning`);
- **красный** — критичные проблемы (`error`).

В popup выводится детальный отчёт: какие теги в порядке, каких не хватает, список замечаний и ошибок.


### Режим фактической серверной проверки

Смысл режима: краулеры соцсетей не видят `localhost` и не исполняют JS. Расширение снимает **набор мета-тегов из исходного HTML страницы** (fetch собственного URL + DOMParser, без исполнения JS — ровно то, что видит краулер) и отправляет его на сервер; если исходник получить не удалось (например, страница — результат POST), используется фолбэк — теги из DOM после JS, с явной пометкой в расширении и на превью-странице. Сервер **не хранит чужой HTML**: он собирает из тегов синтетическую превью-страницу (head с мета-тегами + человекочитаемая таблица тегов, значения экранируются) и раздаёт её по публичному URL. Адрес сервера задаётся в настройках расширения.

Дополнительно локальная проверка сверяет теги из DOM с тегами из исходного HTML: тег, который появляется только после JS (или чьё значение JS меняет), помечается предупреждением — реальные соц-краулеры его не увидят.

**Сохранение картинок.** Чтобы `og:image` были доступны краулеру, картинки могут сохраняться на сервере. Режим сохранения настраивается (3 варианта):

1) **Сохранять все og-картинки** — все изображения из OG-тегов сохраняются на сервер, ссылки переписываются на публичные.
2) **Сохранять только недоступные** — сохраняются только локальные/недоступные извне картинки (например, лежащие на `localhost`); картинки на сторонних ресурсах, доступные публично, не трогаем.
3) **Не сохранять** — раздаётся только HTML, ссылки на картинки остаются как есть.

**Таймер.** Сервер возвращает публичную ссылку, которая отображается в расширении вместе с таймером на 15 минут — фиксированное время жизни статики (TTL в Redis), одинаковое для всех сессий; в настройках расширения не регулируется. Должны быть кнопки продлить время (ещё на 15 минут) и принудительно остановить сессию. По ссылке на статус сервер отдаёт состояние сессии: идёт ли таймер и сколько осталось. Состояние живёт на сервере; расширение при открытии popup восстанавливает его по session id из `storage`, поэтому оно сохраняется при перезагрузке страницы.

**Аутентификация.** Реализуется через куку владельца сессии: продлевать и завершать сессию может только её владелец. Раздаваемая статика изолируется (отдельный домен/поддомен, заголовки CSP, корректный `Content-Type`), вводятся лимиты на размер загрузки и rate limiting.


## Конфигурация проверки

Набор проверяемых тегов и правил задаётся через конфиг. Для каждой соцсети — свой профиль (свой конфиг), что позволяет расширять список соцсетей без изменения кода. Профиль описывает: обязательные теги (их отсутствие → `error`), рекомендуемые теги (отсутствие → `warning`) и дополнительные правила (непустое значение, доступность `og:image` по URL, минимальные размеры картинки и т.п.).

Базовый набор OpenGraph-тегов:

- **Обязательные:** `og:title`, `og:type`, `og:image`, `og:url`.
- **Рекомендуемые:** `og:description`, `og:site_name`, `og:locale`.
- **Для изображения:** `og:image:width`, `og:image:height`, `og:image:alt`.

Профили соцсетей (уточняются в конфиге):

- **Facebook / WhatsApp:** базовый OG + `fb:app_id` (рекомендуемо); `og:image` желательно ≥ 200×200, оптимально 1200×630.
- **VK:** базовый OG; проверка доступности и размеров `og:image`.
- **Telegram:** базовый OG (`og:title`, `og:description`, `og:image`).
- **Twitter / X:** `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image` (с фолбэком на `og:*`).
- **LinkedIn:** `og:title`, `og:type`, `og:image`, `og:description`, `og:url`.

Пример структуры профиля (ориентир, финализируется в коде):

```json
{
  "facebook": {
    "required": ["og:title", "og:type", "og:image", "og:url"],
    "recommended": ["og:description", "og:site_name", "fb:app_id"],
    "rules": {
      "og:image": { "reachable": true, "minWidth": 200, "minHeight": 200 }
    }
  }
}
```

## Состав системы

Система состоит из двух частей - расширения для браузера (Chrome, Firefox) и серверной части.
Серверная часть должна содержать базу данных с данными сессий и бэкэнд.

## Выбор технологий

1) Расширение — **TypeScript**, Manifest V3, `webextension-polyfill` для кросс-браузерности (Chrome, Firefox); сборка — esbuild (4 бандла: background, content, popup, options).
2) Серверная часть — Node.js + **Express** на **TypeScript** (обязательный компонент); в контейнер собирается esbuild-бандл. Раздача статики, CSP (`helmet`), лимиты размера и rate limiting (`express-rate-limit`) реализуются на уровне приложения.
3) База данных — Redis (эфемерные сессии с TTL; при очистке данных не критично).
4) **Reverse proxy — опционален**, добавляется на этапе публичного деплоя, когда нужны:
   - публичный HTTPS (краулеры FB/LinkedIn работают по `https`);
   - изоляция раздаваемой чужой статики на отдельном домене/поддомене от API с кукой владельца.
   Рекомендуемый вариант — **Caddy** (авто-TLS через Let's Encrypt, минимальный конфиг); nginx — как альтернатива. Для локальной разработки/MVP прокси не нужен. Альтернатива для тестирования localhost-страниц — туннель (cloudflared/ngrok), дающий публичный HTTPS-URL без отдельного прокси.
5) Контейнеризация — Docker + docker-compose (Express + Redis; reverse proxy добавляется в compose на этапе деплоя) для простоты развёртывания и масштабирования.

## Открытые вопросы (решены в ходе реализации)

1) ~~Пороги размеров `og:image` и состав тегов~~ — зафиксированы в профилях `extension/src/lib/profiles.ts` (минимумы → error, рекомендуемые размеры → warning).
2) ~~Формат списков доменов~~ — шаблон `host[:port]`, `*` — wildcard, шаблон без порта совпадает с любым портом; по строке на шаблон, `#` — комментарий.
3) ~~Изоляция публичной статики~~ — снята сменой архитектуры: сервер собирает синтетическую страницу из экранированных тегов и не хостит чужой HTML; сверху жёсткий CSP (`default-src 'none'`, скрипты не исполняются). Поддомен для `/s/*` — необязательная опция.

## Задачи разработки (ToDo)

### Этап 0. Подготовка
- [x] Инициализировать репозиторий (monorepo: `extension/`, `server/`, npm workspaces, TypeScript).
- [x] Задать формат конфига профилей соцсетей и наполнить базовыми профилями (FB/WhatsApp, VK, Telegram, Twitter/X, LinkedIn) — `extension/src/lib/profiles.ts`.
- [x] Базовый CI (GitHub Actions: typecheck + build + test) и `.editorconfig`; ESLint/Prettier — опционально позже.

### Этап 1. Расширение — каркас
- [x] Manifest V3, структура: background (service worker), content script, popup, options; сборка esbuild в `dist/`.
- [x] Подключить `webextension-polyfill` для Chrome/Firefox (бандлится в каждый entry).
- [x] Глобальный переключатель вкл/выкл (ползунок) в popup; в выключенном состоянии проверки не идут, badge серый.
- [x] Страница настроек (options): выбор соцсетей, выбор одного из двух режимов области (все кроме чёрного списка / только из белого списка), независимые редакторы обоих списков (не сбрасываются при переключении режима), режим сохранения картинок, адрес сервера. Время жизни сессии не настраивается (фиксировано 15 мин).
- [x] Хранение и синхронизация настроек (`storage.sync` с фолбэком на `local`).

### Этап 2. Локальная проверка
- [x] Парсер OG/Twitter-мета-тегов из DOM (content script).
- [x] Валидатор по профилям соцсетей из конфига: обязательные (`error`) / рекомендуемые (`warning`) теги, пустые значения, доступность и размеры `og:image`; агрегация в итоговый уровень `ok`/`warning`/`error`.
- [x] Применение режима области и активного списка (wildcard, поддомены, порты) — решение, запускать ли проверку на текущем URL.
- [x] Индикация badge: серый (выкл/вне области), синий (в процессе), зелёный (ok), жёлтый (warning), красный (error).
- [x] Popup с детальным отчётом: какие теги ок, каких не хватает, замечания и ошибки.

### Этап 3. Сервер — каркас
- [x] Express-приложение (TypeScript), конфиг через env, Dockerfile (multi-stage, esbuild-бандл) + docker-compose (Express + Redis).
- [x] Подключение Redis, модель сессии (id, ownerToken-хэш, TTL, картинки) — `server/src/store.ts`.
- [x] CORS для расширения; `helmet` (CSP) и `express-rate-limit`; раздача статики средствами Express.

### Этап 4. Серверный режим — API и хостинг статики
- [x] Эндпоинт создания сессии: приём набора мета-тегов, сборка синтетической превью-страницы (чужой HTML не хранится, значения экранируются), картинки по выбранному режиму, переписывание ссылок на публичные, старт таймера (TTL 15 мин).
- [x] Раздача статики по публичной ссылке с изоляцией (жёсткий CSP — скрипты не исполняются, `X-Robots-Tag: noindex`); отдельный домен/поддомен — на этапе деплоя через reverse proxy.
- [x] Эндпоинт статуса сессии: идёт ли таймер и сколько осталось.
- [x] Эндпоинты продления и принудительной остановки таймера.
- [x] Аутентификация владельца: кука + заголовок `X-Owner-Token`; остановка/продление только владельцем.
- [x] Rate limiting и лимиты на количество/размер тегов и картинок (`MAX_TAGS`, `MAX_TAG_VALUE_LENGTH`, `MAX_IMAGES`, `MAX_IMAGE_BYTES`).

### Этап 5. Расширение — интеграция серверного режима
- [x] Кнопка «отправить на сервер», снятие мета-тегов из исходного HTML страницы (без JS — как видит краулер) с фолбэком на DOM после JS и пометкой источника.
- [x] Отображение ссылки и таймера обратного отсчёта в popup.
- [x] Кнопки «продлить» / «остановить».
- [x] Восстановление состояния сессии по session id из `storage` после перезагрузки (запрос статуса с сервера).

### Этап 6. Тестирование и релиз
- [x] Юнит-тесты валидатора (профили/уровни) и логики режимов области/списков (node:test + tsx).
- [x] Интеграционные тесты серверных эндпоинтов (создание/статус/продление/стоп, права владельца, лимиты, переписывание URL) — на in-memory заглушке Redis; прогон с реальным Redis — при ручной проверке.
- [x] Ручная проверка в Firefox на живом стенде (расширение + прод-сервер); в Chrome — повторить перед публикацией в Web Store.
- [x] README: сборка расширения, запуск сервера в Docker (Express + Redis), конфигурация профилей.

### Этап 7. Публичный деплой
- [x] Caddy как reverse proxy с авто-TLS (Let's Encrypt) — `docker-compose.prod.yml` + `Caddyfile`, домен через `.env`.
- [x] Публичный HTTPS на одном домене (API `/api/*` + превью `/s/*`). Изоляция на отдельном поддомене стала необязательной: сервер больше не хостит чужой HTML (синтетические страницы с экранированием); разнос на `api.`/`s.` — опция на будущее.
- [x] Туннель (cloudflared/ngrok) не потребовался — есть постоянный публичный деплой.

### Этап 8. Доработки после MVP (сделано)
- [x] Синтетическая превью-страница из тегов вместо хранения чужого HTML — XSS-поверхность устранена, размеры сессий упали до килобайт.
- [x] Сверка DOM со статическим HTML: предупреждения для тегов, добавленных/изменённых JS (соц-краулеры JS не исполняют); серверный режим шлёт статику с фолбэком на rendered.
- [x] Скрытие превью от поисковиков без поломки соц-краулеров: `X-Robots-Tag: noindex` для не-соц-ботов + robots.txt с явными группами-разрешениями для соц-ботов (Allow-директиву примитивные парсеры не понимают).
- [x] Статистика созданных сессий (день/неделя/месяц/всего) в JSON-файле, эндпоинт `/admin/stats` по токену.
- [x] Access-логи Express и Caddy для диагностики краулеров.
- [x] Оформление UI расширения (тёмная тема, табы Проверка/Сервер, иконки), README (EN/RU), лицензия MIT.
- [x] Локализация приложения (en/ru): выбор языка в настройках, по умолчанию английский; переводятся интерфейс popup/options, сообщения валидатора в отчёте и серверная превью-страница.

### Известные ограничения
- [ ] Telegram-превью для серверов на российском хостинге: ТСПУ режет входящие соединения с подсетей дата-центров Telegram — краулер не доходит до сервера (проверено по логам).
